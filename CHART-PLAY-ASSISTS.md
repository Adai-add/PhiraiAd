# r18 长条渲染数据修复

r18 基于 r17，保留 r14 的触摸、Android 外壳及 FFmpeg 视频能力，沿用原包名与证书，versionCode 10018。

## 原因与修复

r17 在绘制阶段对每帧剩余条身执行 `max(剩余长度, 0.1)`。一旦末段小于下限，尾部就被固定在判定线附近，直到尾部时间到了才整段消失；迟按提示还会使只有头部判定的短长条在到线后停留。

按本次要求，移除这些特殊绘制规则，恢复 r14 的长条绘制实现，仅在入口准备一个供渲染使用的 `NoteKind` 值。只替换它的 `end_time` 和 `end_height`，不复制完整音符，不改变原音符、判定窗口、计分、谱面文件或报告中的原音符数据。

- 显示尾时仍按当前判定模式与播放速度计算：`max(头部时间, 原尾时 - 播放速度 × 尾部免维持窗口)`。
- 在显示尾时查询判定线高度动画，得到缩减后的完整长条高度。
- 最短完整条身改为 **0.01 游戏绘制单位**，不是 0.01 秒。将最小完整长度换算回尾部高度后提交，当前判定线高度不参与该下限计算。
- 原渲染器照常计算剩余长度、绘制资源包头部/条身/尾帽并按显示尾时结束。剩余长度允许小于 0.01，不添加保持下限、进度补偿或自定义尾帽裁切。
- 只有头部判定的长条，到线前按 0.01 完整条身显示，到头部时间即由原结束规则隐藏；不为迟按窗口额外留下条身。实际迟按判定窗口仍然存在。
- 对于完整几何长度被补到 0.01 的极短条，仍按原显示尾时隐藏，不为最小几何长度延长显示时间或判定时间。
- 假音符及关闭缩减时提交原始类型数据。零速度无法显示非零条身，此时避免除零，不人为制造移动。

## 使用与成绩

当前入口为 **设置 → 改版 → 长条缩减**，全局保存，默认关闭，新的游玩或预览使用当前全局配置。改版不上传官方成绩；正常手动游玩可以更新本地最佳记录（r21.3-bugfix-1 修复）。

自动倒打仍在谱面菜单中编辑和保存，在正常与练习模式生效，进度条以绿色开始、红色结束。区间编辑、谱面持久化、旧版配置兼容与上传限制沿用 r17。

## 本次文件与接口

| 文件 | 职责与变更 |
| --- | --- |
| `prpr/src/chart_play.rs` | `MIN_SHORTENED_HOLD_BODY = 0.01`；`hold_render_end_height(head, sampled_end, drawing_scale)` 仅计算完整长条的渲染尾部高度；移除 `hold_body_top`。`hold_visual_end` 保持原安全放手时间计算。 |
| `prpr/src/core/note.rs` | `Note::render` 为绘制准备独立 `NoteKind` 值；`render_with_kind` 接收该值，函数体除类型输入替换外与 r14 渲染器完全相同。移除剩余条身钳制及额外迟按绘制规则。 |
| `phira/locales/*/settings.ftl` | 更新全局开关说明为完整条身最短 0.01。 |
| `prpr/src/play_report.rs` | 报告版本更新为 `0.8.2-replica.18`。 |
| `scripts/package_android_release.py` | 默认版本更新为 `0.8.2-replica18` / 10018，保留原签名与视频验证。 |
| `tests/chart-play/src/lib.rs` | 最小完整长度、末段自然缩短、头部结束、非线性高度取样及退化速度回归。 |
| `tests/verify_hold_renderer.py` | 把函数名与输入名替换还原后，以 r14 源码摘要验证绘制函数体完全一致。 |
| `ANDROID-RELEASE.md`、`MOD-README.md`、本文件 | 构建、版本和实现说明。 |

## 验证与交付

24 项宿主测试通过，覆盖生产配置、数据转换、动画、倒打和持久化。渲染函数体与 r14 的一致性检查通过。Android ARM64 视频版构建及原证书 v1/v2/v3 签名验证结果见随包 verification.json。

尚未进行手机实测；宿主测试不等同于 GPU 画面和触控验证。

完整源码包包含工程及 FFmpeg 静态库；仅修改文件包相对 r17。主项目及测试 Cargo.lock 均未改动。工具链、基础 APK 和私人签名材料不嵌入源码包。
