# r19.1 自定义 RKS

r19.1 基于 r19。保留 r18 长条修复、全局长条缩减、自动倒打、r14 触摸与视频基线。版本为 0.8.2-replica19.1，versionCode 10020，ARM64，沿用原证书。

## 入口与外观

本地谱面库的搜索框左侧新增 `RKS: 15.84` 按钮。普通模式沿用原分类栏、四列卡片、卡片大小、封面、歌曲名和列表滚动位置。

- 卡片左上角：绿底白色对钩表示加入自定义 RKS 候选。触摸正方形宽、高从 0.05 增至 0.065（各增加 30%，向四周各扩展 0.0075），绘制大小仍为 0.05。
- 右上角定数下方：实际使用的准确率，固定两位小数，例如 `98.76%`。采用自定义准确率时显示绿色，即使与记录相同；切回跟随记录时恢复原色。没有记录或没有有效自定义值时显示 `—%`。
- 定数最多一位小数，例如 `16.3` 或 `16`，不会显示 `16.30`。采用值和谱面原定数不同时仅数值显示绿色；`IN Lv.`、`HOT.15 Lv.`、`INS Lv.` 等原前缀不变。自定义值与原值相同则保持原色。
- 总 RKS 和单曲 RKS 显示两位小数。计算使用原始准确率，不把显示值提前四舍五入。

默认全部本地谱面加入候选。候选中缺少数据的谱面暂不产生计算结果，具体缺项可在详情中查看。绿勾不代表一定进入所取的最佳成绩列表。

## 编辑流程

点击 RKS 按钮进入设置模式，顶部显示“本地谱面 / 额外条目 / 计算规则 / RKS / 完成 / 搜索”，本地谱面和额外条目栏另有“全选 / 全取消”。左侧原分类保留显示，在设置模式中锁定本地库，避免切入在线列表。

“全选 / 全取消”只改变当前栏的是否计入：本地谱面栏作用于全部本地谱面，不受搜索或收藏夹过滤；额外条目栏作用于全部额外条目，跨分页生效。两栏互不影响，保留定数、准确率及来源，统一保存并重算。计算规则栏不显示这两个按钮。

本地谱面设置模式中，点卡片左上角切换是否计入，点其余区域打开右侧详情。详情以覆盖层展开，不改变原四列卡片的尺寸和排布；可继续点击左侧露出的卡片切换详情。普通模式点击谱面照常进入游玩。列表开始滚动时取消待处理的勾选点击。

右侧详情提供：

- 是否计入。
- 定数来源：“跟随文件 / 自定义 / AI·待接入”。AI 为预留入口，点击提示尚未接入，不生成结果，不更换当前定数来源。
- 自定义定数输入，保存时四舍五入到一位小数。
- 准确率来源：“跟随记录 / 自定义”。记录使用 Phira 当前存储的本地最高准确率；它本身独立于最高分保存。
- 自定义准确率输入，0～100%，保存到两位小数；跟随记录保留原始精度。
- 单曲 RKS 或缺项提示。

有效修改即时保存并重算；输入非法值时显示错误并保留旧配置。输入框请求绑定原谱面路径或额外条目 ID，切换详情不会把返回的输入写到另一张谱面。

返回键先关闭详情，再退出设置模式；“完成”直接退出设置。新游玩记录、返回谱面库、导入或更新谱面时刷新跟随数据。搜索与收藏夹仅改变显示列表，不限制参与 RKS 的整体本地候选范围。

## 额外条目

二级栏“额外条目”可添加、修改和删除“歌曲名—定数—准确率”，也可切换是否计入。采用分页，避免大量条目挤出手机屏幕。

这些条目不创建实际谱面，不伪造本地游玩记录，不写入原谱面文件。不同条目即使同名也具有独立 ID。新增条目初始为“自定义歌曲 / 0 / 100.00%”，填写实际数据后参与计算。

## 计算规则

设定数为 D，准确率 A 使用百分数（0～100）：

- A < 70：单曲 RKS = 0。
- A ≥ 70：单曲 RKS = D × ((A − 55) / 45)²。

默认独立取最高的 27 个单曲 RKS，以及实际准确率恰为 100% 的最高 3 个单曲 RKS，两组求和再除以 30。AP 可以同时入选两组；不足的槽位按 0 补足。

“计算规则”中可分别设置最佳成绩数量和 AP 数量，范围均为 0～1000，不能同时为零。分母始终为两项配置数量之和。默认 27+3，也可以设成 19+1 等组合。

AP 由未四舍五入的实际准确率判断。比如跟随记录的 99.9999% 即使显示 100.00%，也不会进入 AP 组。手动填写 100.00% 则作为自定义 AP 候选。

## 保存与边界

配置位于应用私有根目录的 `custom-rks.json`，版本字段为 1。使用同目录临时文件、同步和替换保存，不直接截断旧配置；无法保存时不提交内存修改。损坏或未知版本配置报错，不自动覆盖。

每张真实谱面使用本地路径作为配置键，不按歌曲名关联，因此同名谱面和不同难度互不覆盖。删除谱面后其旧设置不会参与当前计算；旧配置保留，若重新使用同一路径导入则可恢复。额外条目使用独立整数 ID。

本地 RKS 与 Phira 账号 RKS 完全独立，不修改账号显示数据、不上传自定义数值、不改真实成绩、不改谱面文件，也不会由于自定义 RKS 设置而改变原有上传资格。r18 原先对长条缩减、倒打等功能的成绩限制继续生效。

## 文件与接口

| 文件 | 总体职责与接口 |
| --- | --- |
| `phira/src/custom_rks.rs` | 与界面无关的数据模型和计算。`Settings::load/save` 读写独立配置；`resolve` 解析单谱面来源；`calculate` 返回按路径索引的显示值和总结果；`Resolved::accuracy_custom` 提供卡片准确率颜色依据；`level_prefix` 保留原难度前缀；`format_difficulty` 输出最多一位小数。 |
| `phira/src/page/rks.rs` | 本地谱面库内的 RKS 控制器、顶部工具栏、右侧详情、额外条目和规则编辑。`refresh` 刷新真实数据；`action` 接收卡片操作；批量命令在一次保存中切换本栏所有条目；`input` 处理绑定目标的文本返回；`back` 管理返回层级；`render_entry/header/overlay` 绘制各部分。 |
| `phira/src/charts_view.rs` | 在原卡片内绘制绿勾、定数和准确率；设置模式分离勾选和详情点击。`rks_values` 共享只读显示快照，`rks_action` 向上返回点击意图。 |
| `phira/src/page/library.rs` | 挂接控制器、模式切换、搜索、返回、记录刷新、卡片事件和渲染层级；保持普通谱面库原布局。 |
| `phira/src/lib.rs`、`phira/src/page.rs` | 注册新增模块。 |
| `prpr/src/play_report.rs` | 报告版本更新为 `.19.1`，不改变报告计分内容。 |
| `scripts/package_android_release.py` | 更新默认 APK 版本，保留视频、JNI、签名和对齐验证。 |
| `tests/chart-play/src/lib.rs` | 生产计算、来源覆盖、AP 精度、去重、额外条目、保存恢复和写入失败回归。 |
| `tests/chart-play/tests/rks_ui.rs` | 运行生产 RKS 编辑器，以记录式图形/输入接口验证设置流程、输入目标绑定、重启持久化和截图比例下的按钮位置。 |

快照只在配置或本地资料刷新时重算；卡片绘制按路径读取缓存，不在每帧扫描所有成绩、读取文件或进行全库排序。

## 验证

41 项测试通过：原有 24 项、RKS 数据与持久化 11 项、编辑器与来源标记 6 项（新增批量选择和准确率来源回归）。后者使用替代图形和输入接口，不是 Android 实机或 GPU 截图验证。另验证了长条绘制函数仍与 r18/r14 基线一致。

ARM64 视频版构建、原证书 v1/v2/v3 签名、APK 对齐和资源校验见随包 verification.json。尚未进行手机实测。

完整源码包含完整工程及 FFmpeg 静态库；仅修改文件包相对 r19。主工程和测试 Cargo.lock 未改变。私人签名材料和编译工具链不在源码包中。
