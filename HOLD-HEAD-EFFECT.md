# r20.0 长条头部打击特效

基于 r19.1，保留自定义 RKS、长条缩减和自动倒打等已有功能。版本名 `0.8.2-replica20.0`，versionCode `10021`，ARM64 视频版，沿用原证书。

## 使用

在“设置 → 谱面 → 长条头部打击特效”切换，默认开启。升级时旧配置没有此字段也会默认开启；手动关闭会持久保存。

- 长条头部成功判定为 Perfect / Good 时，立即显示一次打击特效。
- 特效沿用 Tap 使用的资源包效果与对应 Perfect / Good 颜色；谱面自定义特效颜色仍优先。
- 位置取音符当前横坐标、判定线局部纵坐标 0，再应用判定线完整变换，跟随移动、旋转和倒打。
- 同时支持 Phira / Phigros 判定、触摸、键盘和自动播放。提前按住但头部还未判定成功时不触发；没有成功头部事件时不触发。
- 不在持续按住的每一帧重复触发头部效果；显示头部效果后，将下一次原有周期特效安排到原来的周期之后，避免紧接着双闪。后续长条持续特效继续生效。
- 关闭本开关恢复原有周期特效行为；关闭全局打击粒子效果时，本效果同样不会显示。

本功能只影响视觉，不改变判定窗口、按住时长、计分、RKS 或成绩上传资格。

## 修改文件与接口

| 文件 | 职责与接口 |
| --- | --- |
| `prpr/src/config.rs` | 新增 `Config::hold_head_effect`，序列化为 `holdHeadEffect`，默认 `true`；不加入限制成绩的条件。 |
| `phira/src/page/settings.rs` | 谱面设置中新增开关，复用原设置保存流程。 |
| `phira/locales/*/settings.ftl` | 新增开关标题和说明，简体/繁体中文及英文回退。 |
| `prpr/src/judge.rs` | 五处成功头部提交位置记录本帧事件；`emit_hold_head_effects` 消费事件，使用完整判定线变换和原粒子发射器播放。人工判定和自动播放分别在本帧处理完毕后调用。 |
| `prpr/src/core/note.rs`、`prpr/src/core.rs` | 在 crate 内共享原周期特效间隔，避免复制数值；原长条绘制函数保持一致。 |
| `prpr/src/play_report.rs` | 报告版本更新为 `.20.0`。 |
| `scripts/package_android_release.py` | 更新默认版本名和 versionCode，保留视频、原证书签名、对齐及资源检查。 |
| `tests/chart-play/src/lib.rs` | 新增旧配置迁移、默认开启、关闭后持久化及成绩上传条件回归。 |

没有新增常驻事件缓存；头部特效事件仅在本次判定更新中保存，处理后释放，练习跳转不会遗留上次事件。

## 验证与交付

42 项宿主测试通过。另核对了五条成功头部路径、去除视觉新增部分后的判定代码与 r19.1 一致，以及原长条绘制函数哈希一致。

构建为 ARM64，显式启用 FFmpeg video 功能；使用官方 apksigner 校验原证书 v1/v2/v3 签名。具体安装包验证结果见随包 verification.json。

尚未进行手机或 GPU 实测，需实机确认不同资源包下的观感。

提供完整源码和相对 r19.1 的仅修改文件包。主工程 Cargo.lock 不变；私人签名材料、构建工具链和编译缓存不包含在源码包中。
