# r22.2 AI 定数预测与本地索引

基于 r22.1-bugfix-1，保留 Flick 修复、本地成绩修复和此前功能。模型来源为用户提供的 `Phigros-Phira-Inference-Kit-1.0.0.zip`。

## 使用

- 在本地谱面库的 RKS 编辑界面，将某张谱面的定数来源选为 **AI 计算**，即加入优先预测队列。全局自动开关关闭时也可以这样单独预测。
- 唯一新增设置为 **设置 → 改版设置 → 判定与长条 → 自动预测所有谱面**，默认关闭。开启后，自动处理当前设备本地库中的其余谱面；不会下载未安装的在线谱面。
- 定数预测只用谱面原始内容，不读取歌曲名、原定数、玩家成绩，也不受练习倍速、流速、倒打等游玩开关影响。
- 已选择 AI 的谱面优先；有效 AI 定数统一使用绿色数字，包括与文件原值相同的情况，准确率与 RKS 保留两位小数，定数最多一位小数。
- 等待、预测中和失败状态显示在定数下方。预测未完成或失败时暂不计入该条 RKS，不以 0 或原文件定数冒充 AI 结果。

## 五模型与数值

固定使用 **seed42 的 fold1～fold5**，每张谱面全部运行五个模型后等权平均。没有随机选模、OOF 自动切换、函数校准或输出裁剪。这个固定五模型方案按本次需求选择，与原推理包的开发集 OOF/未知谱 all15 默认选择不同，不宣称是折外评估结果。

Rust 原生实现音符编码、同时组编码、32 组核心块及 4 组上下文、Transformer、GRU、双头门控注意力和整谱统计分支。使用每模型独立的标准化参数，网络 float32，特征统计和最终均值 float64。权重与元数据 SHA-256 校验后加载，单线程内复用。

缓存保存原始五个预测及未舍入均值；RKS 按已有的定数规则取一位小数，卡片显示与计算使用的定数保持一致。

## 后台执行

只创建一个后台工作线程。工作分块检查取消/暂停状态，约每 2 ms 工作后按已用时间让出约 9 倍时间；这是软件调度目标，不是手机总 CPU 占用承诺。模型推理不占用 UI 和判定线程。

主菜单与谱面详情提供活动心跳，进入游玩立即撤销心跳；界面不活跃时，当前任务在下一检查点暂停。后台开关关闭后，非 AI 请求任务中止；手动选择 AI 的任务保留。新 AI 请求可在后台任务的下一检查点抢占，未完成的后台谱面稍后重算，绝不把少于五个模型的中间结果保存成最终预测。RKS 界面最多每 250 ms 合并刷新一次计算结果。

## 持久化与失效

JSON 谱面在顶层增加 `phiraAiDifficulty`；PEC/PBC 在同一谱面目录的 `info.yml` 增加同名字段。内置只读谱面会在其原本的本地目录保留一份用于预测的谱面副本并写入缓存。

缓存包含实现版本、模型资产校验值、谱面字节数与解析选项、五个单模型值及平均值。为兼容旧数据，`chartHash` 字段名保留，但新值为 `bytes:文件字节数:格式:RPE速度选项`，不再计算谱面内容哈希，也不使用文件修改时间。模型资产的 SHA-256 校验仍保留，和谱面变更检查无关。原文件定数不被覆盖。

另在应用数据目录保存 `ai-difficulty-index.json`，与 `custom-rks.json` 同级。索引记录谱面标识、包内相对路径、字节数、解析选项及完整预测。启动时读取小索引并检查文件大小即可恢复定数；不读取谱面正文、不进入模型预测队列。RKS 可直接使用恢复的值。后台再检查 info.yml 的谱面路径与解析选项，出现变化时使对应缓存失效。

首次升级时旧缓存没有字节数，需读取一次谱面；模型版本、五个预测和均值校验通过后，沿用旧结果，按当前字节数迁移并建立索引，无须重新跑五模型。后台先读取可复用缓存，再处理缺失预测，避免被某张大谱面的推理挡住其他缓存恢复。

字节数相同即视为未改变：修改内容但保持字节数不变时，继续复用原定数，这是本版按用户要求采用的规则。首次迁移也以旧缓存和当前文件作为基准，不再核对旧内容哈希。字节数变化、模型版本变化或解析选项变化才重新预测。

JSON 内嵌缓存会改变谱面大小，保存时用计数序列化求出包含缓存在内的最终字节数，再写入缓存和索引，避免自身写入触发循环预测。PEC/PBC 的结果写入 info.yml，谱面正文大小不变。内置谱面副本另记原资源字节数，避免资源大小与内嵌缓存后的大小混淆。

写入使用同目录临时文件、同步和原子替换；提交前重新检查路径、解析选项和字节数。缓存写入保留其他 JSON/YAML 字段。索引损坏时回退到谱面内嵌缓存，重建索引；缓存的模型或数值校验失败时重新预测。

## 格式

Phigros format3 使用推理包规范的 float64 精确提取器；format1 先转换其旧坐标编码。RPE、PEC、PBC 经 Phira 自带解析器转为相同的 17/6/18 维输入，统一使用训练画面比例 1.777778，保留线运动、旋转、父子关系、长条和打击前 0.15 秒锚点；假音符不参与有效按键统计。RPE 的图像、文本绘制和自定义音效不需要加载。

非 format3 的曲线速度由引擎高度曲线的局部导数转换，属于格式适配；原推理包对这些格式没有提供训练评估，不能将其预测精度等同于原版谱面。畸形事件、空谱面、无效数字、路径越界或过大输入返回错误，不自行补出定数。单张谱面输入上限 127 MiB、200,000 音符和 10,000 判定线。

## 文件与接口

| 文件 | 职责/主要接口 |
|---|---|
| `phira/src/ai_model/mod.rs` | `Predictor::new/predict`、五模型版本与校验、`Prediction::valid` |
| `phira/src/ai_model/features.rs` | `extract` 精确原版解析；`from_notes` 统一特征统计；`Features::validate` |
| `phira/src/ai_model/network.rs` | 权重读取、标准化及完整前向推理 |
| `phira/src/ai_model/cache.rs` | `Source::probe/key`、`Document::load/cached/save`；字节数键、旧缓存迁移、原子写入与竞争修改检查 |
| `phira/src/ai_model/index.rs` | `Index::load/save/insert`、`Record::restore`；启动索引恢复、相对路径与字节数检查 |
| `phira/src/ai_model/scheduler.rs` | `order/allowed` 优先顺序和自动开关语义 |
| `phira/src/ai_model/assets/` | 固定五模型的 JSON/BIN 及索引 |
| `phira/src/ai_chart_adapter.rs` | 非 format3 格式适配，无图像和音频加载 |
| `phira/src/ai_service.rs` | 后台线程、限时工作、活动心跳、队列、状态快照和缓存复用 |
| `phira/src/custom_rks.rs`、`page/rks.rs`、`page/library.rs` | AI 来源选择、定数接入、状态与合并刷新 |
| `prpr/src/config.rs`、`phira/src/page/settings.rs` | 唯一全局自动开关及布局 |
| `prpr/src/info.rs` | 保留文本/二进制谱面包的 AI 缓存元数据 |
| `phira/src/scene/main.rs`、`song.rs`、`chapter.rs` | 活动/暂停通知及内置谱面兼容 |
| `tests/ai-inference/`、`tests/ai-vectors/` | 参考向量、五模型输出、缓存、取消与队列测试 |
| `tools/ai-verify/` | 使用真实 Phira 解析器的格式验证程序 |
| `tests/chart-play/` | 原功能及 AI 来源设置的 UI 回归 |

预测模块的 `checkpoint` 回调可以暂停或返回错误取消。调用者应在后台线程运行完整提取与推理，只有五个模型全部完成后才能提交 `Prediction`。

## 验证命令

```bash
cargo test --locked --manifest-path tests/ai-inference/Cargo.toml
cargo test --locked --manifest-path tests/chart-play/Cargo.toml
cargo run --locked -p phira-ai-verify
```

测试向量比较 17 维音符、6 维同时组、18 维统计及组时长，再比较五个单模型输出，允许绝对误差 0.0001。最终交付的构建验证文件记录实际测试数量、签名、视频标记及安装包差异。尚未实机测量 nova 10 Pro 的性能和功耗。

## 内存与验证

谱面内容哈希已移除。保留 r22.1 的内存优化：解析 JSON 后释放原始文件字节，提取特征后释放解析树，推理完成后释放特征再重新校验源文件；JSON 缓存通过带缓冲的临时文件直接写出，不创建整份输出字节数组。大小固定点计算只统计输出字节数，不保存序列化副本。

127 MiB 是单个谱面文件大小上限，并非内存占用上限；本次未改成全流程流式解析，RPE/format1 格式适配仍可能暂时复制解析树。原有 200,000 音符、10,000 判定线限制未调整。

r22.2 的 13 项自动测试覆盖索引重新加载及非零 RKS 恢复、同字节数修改复用、大小变化与删除失效、旧缓存迁移、模型及路径检查、损坏索引回退、超过 32 MiB 实际预测、127 MiB 边界、缓存写入和原子写失败保护。PGR3/PGR1/PEC/RPE/PBC 原生解析器的五模型输出与 r22.1 完全一致。未进行手机重启耗时实测，不承诺固定毫秒数。
