# r23.0-bugfix-1 Mods 入口与倒打编辑界面修复

- 按键转换移至每谱面的 Mods 侧栏，全 Tap / 全 Drag / 全 Flick 三项互斥；再次点击已开启项回到原键型，切换立即保存。保留 r23.0 已保存的选项和倒打区间。
- 自动倒打编辑界面只保留区间编辑功能，恢复紧凑排版；绘制相机使用 Ui 的完整屏幕视口，独立于谱面裁切的宽高比，并在绝对 UI 坐标下绘制，触摸区域与屏幕显示一致。编辑模式不再叠加打歌 HUD。
- 根因：r23.0 编辑器 camera 的 viewport 使用完整屏幕，而 zoom 通过当前谱面 viewport 获取宽高比。宽屏手机上两个比例不同，会拉伸 UI 并使命中位置错开。新代码由同一个 Ui::camera 提供视口和投影。
- 保留 r23.0 的 0.8 秒旋转、原暂停/恢复路径、全局倒打开关及运行时音符转换。

修改接口：`ChartPlayEditor::camera` 隔离编辑相机；`NoteConversionMods::render/touch` 复用 Mods 侧栏的按钮触摸方式；`chart_play_settings::load/save` 让 Mods 与倒打编辑器共享同一份谱面配置，并保留其它字段。

81 项宿主测试通过：新增实际按钮命中代码的像素点击回归（20:9、16:9、4:3 和更宽屏），Mods 互斥及恢复原始模式、JSON/PEC/内置谱面保存重读。宿主测试不能替代真实字体、GPU 和手机触摸验证；尚未实机测试。

版本名 `0.8.2-replica23.0-bugfix1`，versionCode `10033`。以下为历史说明，入口以本节为准。

# r23.0 自动倒打旋转与按键转换

基于 r22.3；版本名 `0.8.2-replica23.0`，versionCode `10032`。

- 设置 → 改版 → 谱面：新增自动倒打总开关，默认开启以兼容已有区间；关闭后保留各谱面的区间配置。
- 谱面菜单“自动倒打 / 按键转换”：预览中新增、删除立即生效，选中已有区间后标记起止或微调立即更新。重叠区间仍合并，保存并返回才写入，取消放弃本次更改。编辑预览不受总开关限制。
- 正常及练习播放到区间起止，调用与暂停按钮共用的音乐、时钟和触摸清理逻辑，隐藏暂停菜单，用真实时间 0.8 秒平滑旋转谱面画面 180°，再走 Phira 原恢复逻辑（包含原有回退及倒计时）。进区间旋转到倒置，出区间旋转回正。暂停菜单、HUD 和编辑控件保持正向。
- 倒置后的触摸坐标同时旋转两轴。特效、粒子与谱面合成后统一旋转。暂停拖动练习位置直接同步朝向；重试清空边界状态；旋转中切后台保持暂停，用户手动继续。
- 每谱面按键转换：原始 / 全 Tap / 全 Drag / 全 Flick 四选一，普通音符与长条头统一转换，长条尾不生成额外音符；时间、位置、假键属性保留，转换使用对应默认音效。切回原始恢复类型和自定义音效，重建绘制缓存与判定器。
- 设置写入既有 replicaPlay 配置：JSON 谱面本体、PEC/PBC 的 info.yml，内置谱面沿用独立配置路径。原始音符数据不改写，不改 AI 模型输入。
- 继续沿用改版禁止官方成绩上传、本地记录与 RKS 的既有规则，以及 r22.3 AI 标色和练习音高修复。

新增 `prpr/src/auto_flip.rs` 管理边界与旋转状态；`NoteConversionBackup` 保留原始类型和音效；`JudgeLineCache::with_companion` 重排缓存时同步保留音符身份；生产暂停/恢复代码抽成共用方法。宿主测试覆盖真实编辑控件、缓存、暂停恢复函数及边界状态；不等同于手机 GPU、触摸或音频实测。

以下保留历史版本说明。

# r22.3 AI 标色与练习音高修复

- 本地库中的有效 AI 定数统一绿色。只改变定数数字颜色，难度和 Lv. 前缀、准确率规则、RKS 数值与索引均保持原行为。待预测/失败时不把文件定数标成 AI 结果。
- 练习暂停界面左上新增“调整速度时修复音高”，默认关闭，选择保存到应用配置 `practicePreservePitch`。开启时，变速音乐保持原音高；继续或重试时应用修改，即使只切换开关而没改速度也会更新音频。
- 仅作用于练习模式音乐，不改音符流速、判定、按键音效或其他模式。1.00 倍速使用原播放路径。关闭后沿用原来的变速变调。
- 采用 SoundTouch 2.3.3 的 tempo 处理；固定小块从已有解码音频预读，不生成一份完整的慢速音频。暂停不推进曲目时间；跳转清空旧缓冲，输出采样率变化会重置处理器并保留曲目位置。位置接口始终以原曲秒数报告，未把预读到的位置用作判定时钟。
- 极低/极高倍速下可能有重复感或瞬态变化，不承诺音色完全无损；手机实际音质、性能与触摸布局尚未实测。

## 新增和修改接口

| 文件 | 职责与接口 |
| --- | --- |
| `phira/src/custom_rks.rs` | `Resolved::difficulty_ai` 表示当前有有效的 AI 定数；不改 `difficulty_changed` 的原含义。 |
| `phira/src/charts_view.rs` | AI 定数或与文件不同的定数使用绿色数字。 |
| `prpr/src/config.rs` | `Config::practice_preserve_pitch`，默认 false，支持旧配置自动补默认值。 |
| `prpr/src/practice_audio.rs` | `Music::new/position/paused/play/pause/seek_to/preserves_pitch`；原播放与修复播放的统一接口。`PREFERENCE_SAVER`/`save_preference` 在 UI 线程保存开关。 |
| `prpr/src/practice_soundtouch.cpp` | 创建、销毁、输入、输出、重置和结束的窄 C ABI，C++ 异常不穿越 Rust 边界。 |
| `prpr/build.rs`、`prpr/vendor/soundtouch/` | 固定版本音频库的构建、完整库源码及许可证。 |
| `prpr/src/scene/game.rs` | 练习暂停开关、切换后重建音乐、练习专用启用条件。 |
| `phira/src/lib.rs` | 安装配置保存回调，保存失败回滚。 |
| `prpr/locales/*/game.ftl` | 音高修复选项文案。 |
| `tests/practice-audio/` | 直接编译生产音频代码和 C++ 处理器，检查音高、时长、声道一致性、暂停与跳转时钟。 |
| `tests/chart-play/src/lib.rs` | 新增 AI 同值标绿、等待状态和配置持久化回归。 |

版本名 `0.8.2-replica22.3`，versionCode `10031`。以下保留历史改版说明。

# r22.0 AI 定数预测

新增设置与接口详见 AI-INFERENCE.md。版本名 `0.8.2-replica22.0`，versionCode `10027`。

# r21.3-bugfix-1 本地成绩修复

基于 r21.3，版本名 `0.8.2-replica21.3-bugfix1`，versionCode `10026`。

修复纯本地谱面仍受旧的成绩资格条件限制的问题：开启长条缩减、自动倒打、关闭特效或使用改版判定后，正常手动游玩可保存本地最佳成绩。保存资格与上传资格分开；改版继续禁止官方成绩上传。自动演示、练习、预览、延迟调整和倒打编辑模式不保存最佳成绩。

沿用原有记录格式和最佳成绩更新规则，低分不会覆盖高分，准确率独立取最高值，自定义 RKS 可继续跟随本地最佳准确率。旧版未写入的成绩无法自动恢复，需要重新游玩记录。

变更接口：`Config::saves_run_record()` 不再接受谱面来源和辅助功能参数；`GameScene` 继续按模式筛选后调用原保存回调。记录存储、云端隔离及 21.3 滑条交互不变。

以下保留 r21.3 的界面说明。

# r21.3 练习滑条触控优化

基于 r21.2。版本名 `0.8.2-replica21.3`，versionCode `10025`，ARM64 视频版，沿用原改版证书。

## 本次改动

- 练习速度和音符流速滑条的触摸区域高度由 0.03 扩大到 0.09，左右各留 0.025 的触摸余量；滑块标记略增大。
- 按下滑条后绑定该手指。手指移出滑条、经过数值/加减按钮或另一条滑条时，仍按水平位置调整原滑条，不触发经过的控件。
- 某帧没有该手指事件时继续保留绑定，不再立即结束拖动。其它手指的移动或抬起不结束当前拖动。
- 绑定手指抬起时结束；系统明确取消触摸、应用切后台、暂停界面关闭或进入新场景时清理状态，避免残留拖动。
- 数值字体由 0.42 缩小到 0.36，按标签实际绘制宽度排列，紧跟“速度”“音符流速”之后；数值仍可点击输入。
- 加减按钮边长由 0.05 增至 0.08，按钮间距由 0.01 增至 0.025，滑条与按钮间距增至 0.035。
- 两行垂直间距由 0.14 增至 0.19，滑条视觉长度从 0.50 微调至 0.46，为右侧按钮留出屏内空间。滑条触摸区、数值按钮和加减按钮互不重叠。

以上尺寸均为游戏 UI 绘制单位，不是像素。

## 保留的使用规则

中点仍为 1.00，两侧分别线性。播放速度 0.05～10.00、音符流速 0.10～20.00；拖动与加减按钮步进 0.05，数值输入支持三位小数。手动更改流速解除倒数锁定；更改播放速度时，已开启的倒数锁定继续生效。

在线谱仍默认保存独立本地最佳、不上传官方成绩。改版设置分组、统一判定、长条缩减、自定义 RKS 等功能沿用 r21.2。

## 问题原因与修复

r21.2 的滑条在一帧没有找到对应触摸时将绑定清空；数值按钮又先于滑条处理触摸，可能取走移入数值区域的事件。本版本先处理已绑定手指，再从当前 UI 事件列表移除已处理的手指事件，随后才处理其它控件。没有事件的帧保持当前数值和绑定，收到后续事件时继续更新。

这项修复针对练习设置滑条，不修改打歌时的触摸判定，也不代表此前调查的多指断触问题已修复。

## 文件职责与接口

| 文件 | 职责 / 接口 |
| --- | --- |
| `prpr/src/ui.rs` | `Ui::practice_speed_slider()` 放大触摸区域、捕获拖动手指、隔离控件事件并调整排版；`Ui::clear_practice_speed_drag()` 清理两条练习滑条的拖动和按钮按下状态。 |
| `prpr/src/scene/game.rs` | 调整练习两行间距和滑条长度；在暂停界面关闭、新场景进入与应用暂停时调用清理方法。 |
| `prpr/src/play_report.rs` | 更新报告版本。 |
| `scripts/package_android_release.py` | 更新 APK 默认版本名和版本号。 |
| `tests/chart-play/tests/practice_ui.rs` | 使用实际滑条及按钮代码测试拖动端点、中点、扩大区域、空事件帧、移出范围、越过其它控件、多指隔离、生命周期清理、数值入口和加减按钮。 |
| `ANDROID-RELEASE.md` | 构建签名步骤与验证范围。 |

## 验证与交付

59 项宿主测试通过：41 项配置/算法/数据测试、6 项练习滑条交互测试、7 项 RKS 界面测试、5 项设置分组测试。交互测试使用记录式绘图与输入适配器，不等同于实际字体、GPU 或手机触摸验证。

ARM64 视频版编译通过；判定算法、长条绘制、速度映射和 RKS 算法相对 r21.2 不变。签名、APK 资源/JNI 导出、源码包校验见随包 verification.json。**尚未手机实测。**

提供 APK、完整源码和相对 r21.2 的仅修改文件包。源码不含私人密钥、基线 APK 或工具链；已有设置和成绩记录保留。
